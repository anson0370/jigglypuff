// Generated by CoffeeScript 1.6.3
(function() {
  var FileNotFoundError, app, dataProvider, env, express, fs, render, _;

  console.log("version: 0.0.11");

  _ = require("lodash");

  fs = require("fs");

  env = require("./enviroments");

  render = require("./handlebars/render");

  express = require("express");

  dataProvider = require("./data_provider");

  FileNotFoundError = require("./errors").FileNotFoundError;

  app = express();

  app.get(/^([^\.]+)$/, function(req, res, next) {
    var context, dataResult, err, path, result;
    path = req.params[0];
    dataResult = dataProvider.getUrlData(path, req.query);
    context = _.isPlainObject(dataResult.result) ? _.assign(req.query, dataResult.result) : req.query;
    try {
      result = render.renderFile(path, context);
      return res.send(result);
    } catch (_error) {
      err = _error;
      if (err instanceof FileNotFoundError) {
        return next();
      } else {
        throw err;
      }
    }
  });

  app.get(/^(.+)$/, function(req, res, next) {
    var path, realPath;
    path = req.params[0];
    realPath = "" + env.filesHome + path;
    if (fs.existsSync(realPath)) {
      return res.sendfile(realPath);
    } else {
      return next();
    }
  });

  app.all(/^(.+)$/, function(req, res) {
    var dataResult, path;
    path = req.params[0];
    dataResult = dataProvider.getUrlData(path, req.method, req.query);
    if (dataResult.found) {
      return res.send(dataResult.result);
    } else {
      console.log("[Not Found] " + path);
      res.status(404);
      return res.send();
    }
  });

  app.listen(env.serverPort);

  console.log("server listening at port: " + env.serverPort);

}).call(this);
